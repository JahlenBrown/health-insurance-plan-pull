node_modules/
.env
.env.local
*.log
.DS_Store

# Documents pulled from an authenticated member portal by the user
# themselves (never by this tool -- see docs/MEMBER-PORTAL-DOCUMENTS.md).
# May carry account-specific headers/watermarks; never committed.
data/member-uploads/
